### SIMPLE MATCHING
* # matching any number of arbitrary characters
? # matching a single character

### REGEX
.* # any number of characters
.+ # any one or more characters
.  # one arbitrary character


### COMMAND PARAMETERS
# ls - long listing
ls -l
# ls - filetypes
ls -F
# ls - see dot files
ls -a

# find - name and print
find dir -name filename -print

# sort - numerically
sort -n
# sort - reversed
sort -r

# rm - recursive (dangerous)
rm -r

# grep - case-insensitive
grep -i
# grep - invert search
grep -E
